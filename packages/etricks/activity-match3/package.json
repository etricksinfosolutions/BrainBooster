{
  "name": "@etricks/activity-match3",
  "version": "0.0.1",
  "private": true,
  "description": "Match-3 gameplay activity — pure, headless board logic (deal/match/collapse/swap) extracted from Brain Booster. A reusable activity any game can compose (ADR-0013).",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "default": "./src/index.ts"
    }
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "clean": "rimraf dist .turbo"
  },
  "devDependencies": {
    "rimraf": "^6.0.1",
    "typescript": "^5.7.2"
  }
}
